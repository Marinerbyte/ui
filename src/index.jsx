import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./style.css";   // 👈 file MUST exist at src/style.css

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
